import { createTheme } from "@mui/material/styles";

export const myTheme = createTheme({
  palette: {
    primary: {
      main: "#5E4FDB",
      light: "#F3F2FC",
    },
    secondary: {
      main: "#1ABC9C",
    },
    negative: {
      main: "#D64B4B",
    },
    pureblack: {
      main: "#000000",
    },
  },
});
